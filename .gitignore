# Rust
/target
**/*.rs.bk

# Frontend tooling (once the Tauri shell exists)
node_modules/
dist/

# Real task data must never land in this public repo
*.automerge

# Private notes for local Claude sessions
CLAUDE.local.md

# OS / editor noise
.DS_Store
*.swp
