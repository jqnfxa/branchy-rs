{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell talks to the graph through its own commands and needs nothing else.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
